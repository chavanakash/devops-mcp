apiVersion: v1
kind: Service
metadata:
  name: status-api
  labels:
    app: status-api
spec:
  type: NodePort
  selector:
    app: status-api
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
